const {Triangle, Square, Circle} = require("./shapes")

test("Pick your shape", () => {
    expect(Triangle)
})

test("Pick your shape", () => {
    expect(Square)
})

test("Pick your shape", () => {
    expect(Circle)
})